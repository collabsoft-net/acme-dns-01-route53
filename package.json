{
  "name": "@collabsoft/acme-dns-01-route53",
  "version": "1.0.0",
  "description": "An ACME dns-01 challenge handler for Greenlock",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "start": "npx nodemon --watch src -e '.ts' --exec 'npx yarn build'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "acme-challenge-test": "^3.3.2",
    "aws-sdk": "^2.493.0",
    "bluebird": "^3.5.5",
    "nice-route53": "^1.3.0",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "@types/node": "^12.6.5",
    "acme-dns-01-test": "^3.3.1",
    "nodemon": "^2.0.6"
  }
}
